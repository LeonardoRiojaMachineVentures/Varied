struct Position {
	x : f64,
	y : f64,
}
impl Position {
	fn new(x : f64, y : f64) -> Position {
		return Position {
			x,
			y,
		}
	}
}
fn main() {
	let mut red = Ball::new(0f64, 0.0f64);
	y = -x^2
}
enum Ascii
	A
	B
	C
	D
impl Ascii
	to_num(x Ascii) (Nat)
		match x
			A
				Zero
			B
				
[Ascii] -> [Ascii]
set x Real
let h Real
: h >= 0
let l Real
: l > 0
set y Real
set z Real
set t Set::product(x y z)
: z >= h
: x >= 0
: x <= l

